version: '3.9'

services:
  frontend:
    build:
      context: .
      args:
        VITE_BACKEND_URL: ${VITE_BACKEND_URL:-https://iac-quiz-buzzer-website-production.up.railway.app}
    ports:
      - "3000:80"
    restart: unless-stopped
