version: '3.9'

services:
  frontend:
    build:
      context: .
      args:
        VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:5001}
    ports:
      - "3000:80"
    restart: unless-stopped
